# Run twice manually and compare
python3 -c "
import requests, os
from dotenv import load_dotenv
load_dotenv()
resp = requests.get(os.getenv('API_BASE')+'/executions', headers={'Authorization': 'Bearer '+os.getenv('TOKEN')}, params={'agent_id': os.getenv('AGENT_ID')})
print(resp.json()['items'][0])
"
